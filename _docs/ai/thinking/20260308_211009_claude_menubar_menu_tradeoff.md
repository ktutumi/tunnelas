# メニューバー compact menu の設計判断

- `MenuBarExtra(.window)` を維持するとスクロール前提の情報密度が残るため、ネイティブメニューへ寄せた。
- 新規ファイル追加は `.xcodeproj` 再生成を伴うため、今回の集計ヘルパーは既存ファイルへ寄せて差分を小さくした。
- 詳細表示は別ウィンドウ新設ではなく既存の Logs ウィンドウを再利用し、操作導線の追加だけで済ませた。
