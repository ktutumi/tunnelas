# Tunnelas Bootstrap

- 目的: 新規 macOS メニューバーアプリとして `Tunnelas` の土台を構築する。
- 画面構成: メニューバー本体、ログウィンドウ、設定ウィンドウ。
- データ構造: `config.json` 互換モデル、ルール状態スナップショット、ログエントリ。
- リスク: `ssh` / `kubectl` の存在チェック、スリープ復帰時のプロセス整合、GitHub CLI 認証無効。
- 完了条件: XcodeGen 再生成、ビルド、テスト、基本 UI の表示が成立する。

