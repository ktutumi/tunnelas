# PR #6 Review Fixes

- Codex review で指摘された再起動、終了処理、disabled ルール起動の 3 点を修正する。
- ランタイム側で停止完了待ちを持ち、`restart` と `shutdown` が停止途中のセッションを取りこぼさないようにする。
- グループ起動は disabled ルールを素通りし、意図した無効設定をエラー扱いしない。
- 修正を支える回帰テストを追加し、review 指摘を再発させない状態を作る。
